# Задание 07 Тип исключения

Измените метод
```java
public String getExceptionType()
```
так, чтобы он возвращал тип брошенного в методе `process()` исключения: 
`"checked"` или `"unchecked"`. Или `"none"` если`process()` не бросит никакого исключения. 
